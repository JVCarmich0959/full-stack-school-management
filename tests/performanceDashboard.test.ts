describe("performance dashboard metrics", () => {
  it.todo("should normalize subject metrics once live assessments are wired");
  it.todo("should expose classroom aggregates via websocket updates");
});

describe("useClassroomMetrics hook", () => {
  it.todo("aggregates three mock students and returns average scores");
});
